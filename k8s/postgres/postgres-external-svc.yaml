apiVersion: v1
kind: Service
metadata:
  name: postgres-nodeport
  namespace: default
spec:
  type: NodePort
  selector:
    app: postgress
  ports:
    - name: postgres
      port: 5432        # Redis port inside cluster
      targetPort: 5432
      nodePort: 30555   # Port on the node
